import {MainLayout} from "layouts";
import { TabularMatrix } from "features";

export const MainPage = () => {

    return (
        <MainLayout>
            <TabularMatrix />
        </MainLayout>
    )
}